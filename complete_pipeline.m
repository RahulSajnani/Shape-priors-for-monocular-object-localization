function complete_pipeline()
    
    globals;
    WireframeToImage(frame, seq, frm, id, camH, avgCarLength, avgCarWidth, avgCarHeight, K, Pts, lambda, V, kplookup, numViews,seqId,CarId,startFrame,endFrame,totalFrames);
    
end
    